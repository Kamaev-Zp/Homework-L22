package L17.In.animals;

public interface Drawable {
        //Животное  издает звук  с родительского класса, и так же переопределяется метод в дочерном классе
        void draw();
        //Выводить имя возраст с родительского класса, и так же переопределяется метод в дочерном классе
        void  print();


    }

